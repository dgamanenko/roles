filter {
  if [type] == "uwsgi" {
    grok {
      match => { "message" => "%{UWSGI}" }
      overwrite => [ "message" ]
{% if logstash_filter_tags|length %}
      add_tag => ["{{ logstash_filter_tags | join('\", \"') }}"]
{% endif %}
    }
  }
}
