filter {
  if [type] == "mongo" {
    grok {
      match => { "message" => ["%{MONGO}", "%{MONGO3}"] }
      overwrite => [ "message" ]
{% if logstash_filter_tags|length %}
      add_tag => ["{{ logstash_filter_tags | join('\", \"') }}"]
{% endif %}
    }
  }
}
