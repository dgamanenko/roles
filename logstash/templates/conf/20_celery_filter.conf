filter {
  if [type] == "celery" {
    grok {
      match => { "message" => "%{CELERY}" }
      overwrite => [ "message" ]
{% if logstash_filter_tags|length %}
      add_tag => ["{{ logstash_filter_tags | join('\", \"') }}"]
{% endif %}
    }
  }
}
