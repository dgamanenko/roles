filter {
  if [type] == "snort" {
    grok {
      match => { "message" => "%{SNORT}" }
      overwrite => [ "message" ]
{% if logstash_filter_tags|length %}
      add_tag => ["{{ logstash_filter_tags | join('\", \"') }}"]
{% endif %}
    }
  }
}
