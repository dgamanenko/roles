filter {
  if [type] == "mysql" {
    grok {
      match => { "message" => "%{MYSQL}" }
      overwrite => [ "message" ]
{% if logstash_filter_tags|length %}
      add_tag => ["{{ logstash_filter_tags | join('\", \"') }}"]
{% endif %}
    }
  }
}
